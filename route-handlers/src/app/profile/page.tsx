import React from "react";

export default function Profile() {
  return (
    <div>
      Profile page
      <p>
        page.tsx and route.tsx GET request will always override to get rid of it we can use api
        sub-directory to manage all API&apos;s seperately.
      </p>
    </div>
  );
}
